# Project
Enable anyone to precess parquet/heavy csv files directly on the web, and output an excel file. Built in pure js, few libraries but no framework needed

# Limitations
As a demo there's plenty of it, but first : 
- Duckdb wasm doesn't support httpfs extension, so no http file access possibility
- as of now, no spatial operation possibility
